# 02 ADC Read

Minimal example showing the intended use of the MCP3208 ADC API.

This folder is a placeholder until the first precompiled SDK library release is generated.

Expected APIs:

- `ls_mcp3208_init()`
- `ls_mcp3208_read_sample()`
- `ls_mcp3208_read_all()`
