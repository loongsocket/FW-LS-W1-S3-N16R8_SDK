# 01 Basic Status

Minimal example showing the intended use of the public BMS/USB status API.

This folder is a placeholder until the first precompiled SDK library release is generated.

Expected APIs:

- `ls_bms_init()`
- `ls_bms_get_status()`
- `ls_usb_get_status()`
